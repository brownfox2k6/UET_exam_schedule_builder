name: Build solver

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install macOS OpenMP
        if: runner.os == 'macOS'
        run: brew install libomp

      - name: Install Python build dependencies
        run: python -m pip install --upgrade pip nanobind

      - name: Configure CMake
        shell: bash
        run: |
          cmake_args=(-S solver -B solver/build-ci -DCMAKE_BUILD_TYPE=Release)
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            cmake_args+=("-DOpenMP_ROOT=$(brew --prefix libomp)")
          fi
          cmake "${cmake_args[@]}"

      - name: Build solver
        run: cmake --build solver/build-ci --config Release --parallel
